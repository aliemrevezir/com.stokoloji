/**
 * Tasarım token'larının web içindeki tek erişim noktası.
 * Kaynak: @stokoloji/ui (iki site arasında paylaşılır). Burada yalnızca
 * yeniden ihraç edilir, böylece web kodu kısa bir yoldan token'lara erişir.
 */
export { tokens, colors, fonts, radii } from '@stokoloji/ui/tokens';
export type { Tokens } from '@stokoloji/ui/tokens';
