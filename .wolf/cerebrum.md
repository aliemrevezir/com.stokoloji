# Cerebrum

> OpenWolf's learning memory. Updated automatically as the AI learns from interactions.
> Do not edit manually unless correcting an error.
> Last updated: 2026-06-14

## User Preferences

<!-- How the user likes things done. Code style, tools, patterns, communication. -->

- İletişim **Türkçe**, kısa, aksiyona dönük; samimi ton ("knkm/kanka"). Listeden çok prose; karar öncesi kısa sparring/devil's advocate, sonra net küçük adımlar.
- **Hız için kaliteden taviz yok**; sustainable çözüm, hack değil. Thin/yığın AI içerik kırmızı çizgi.
- **Secret'ları düz metin memory dosyasına YAZMA** — password manager + Coolify env. Kullanıcı bunu açıkça istedi; sadece "şuraya saklanıyor" işaretçisi kabul.
- Commit'leri **anlamlı, gruplu** ister (feat/fix/chore/docs, açıklamalı gövde). Commit/push'u açıkça söyleyince yap.
- Değişikliği körlemesine commitleme — kullanıcı (veya başka araç) dosya düzenlemiş olabilir; önce diff'e bak.
- UI metinleri + içerik **Türkçe**, teknik terimler İngilizce. **Uzun çizgi (em dash) YASAK.**

## Key Learnings

- **Deploy artık GHCR üzerinden (2026-06-19):** Build VPS'te DEĞİL, GitHub Actions'ta (`.github/workflows/build-push.yml`) → `ghcr.io/aliemrevezir/stokoloji-{web,cms}:latest+sha` push. `docker-compose.prod.yml` `build:` yerine `image:` kullanır; Coolify pull eder (build etmez). Sonuç: 26 dk + OOM → birkaç dk. web build args (`NEXT_PUBLIC_*`) **GitHub repo Variables**'ta (Secrets değil; `${{ vars.* }}`). cms build-arg almaz (Strapi secret'ları runtime/Coolify env). Yeni `NEXT_PUBLIC_*` değeri eklenince hem Dockerfile ARG/ENV, hem compose, hem workflow build-args, hem repo Variable güncellenmeli.
- **GHCR private image + Coolify "not found" (GOTCHA):** GHCR paketleri varsayılan private; yetkisiz pull'da "unauthorized" değil **"not found"** döner (yanıltıcı). Çözüm — paketi public YAPMA (private repo kodunu image üzerinden ifşa eder); bunun yerine VPS'te bir kez `echo <PAT> | docker login ghcr.io -u aliemrevezir --password-stdin` (PAT = classic, `read:packages`). Coolify host docker daemon'ı kullandığı için login kalıcı; redeploy pull eder. Auto-deploy webhook image build bitmeden tetiklenirse de geçici "not found" olur → CI bitince redeploy.
- **İçerik artık MARKDOWN, panelden düzenlenir (FAZ 2, 2026-06-19):** Blog `icerik` + tool `formulAciklamasi` alan tipi `json` → **`richtext`** (Strapi 5 native Markdown editörü; string saklar). Web render `BlocksRenderer` DEĞİL artık `MarkdownContent.tsx` (react-markdown + remark-gfm tablo + remark-math/rehype-katex formül; başlık id'leri `slugify` ile, rehype-slug DEĞİL → TOC anchor tutarlı). Tablo = GFM `| |`, display formül = `$$...$$` (LaTeX) / ```formula fenced (kelimeli, formül kartı). **Artık panelden metin/tablo/formül düzenle → deploy GEREKMEZ, ISR ile canlıya gelir.** `BlocksRenderer` hâlâ duruyor: `sozluk.anlam` (blocks) + MarkdownContent'in dizi-fallback'i için.
- **Blocks→Markdown dönüşümü ve panel-kalıcılığı (KRİTİK):** Converter `seed/blocksToMarkdown.ts` (`blocksToMarkdown` + `contentToMarkdown`). Seed (CREATE-only) artık `icerik: blocksToMarkdown(SDH_BLOG)` ile markdown string yazar. Canlı DB'yi `index.ts` `migrateContentToMarkdown` BİR KEZ çevirir: `contentToMarkdown(value)` dizi / `"[...]"` JSON-metni → markdown'a çevirir, **zaten düz markdown ise `null` → DOKUNMAZ**. Böylece dönüşüm idempotent + tek-seferlik; bir kez markdown'a döndükten sonra panel düzenlemeleri ezilmez. Converter `/blog/→/icerik/` link fix + FAQ başlığı strip'i de içerir. `migrateStokDevirHiziBody` KALDIRILDI (artık gövdeyi ezmeyen mimari). `migrateFormulaBlocks`/`migrateInternalLinks`/`syncBlogFaq` body-strip'leri `Array.isArray` guard'lı → markdown string'de no-op olur (zararsız), `syncBlogFaq` sss senkronu kalır.
- **GOTCHA — schema json→richtext cast (deploy öncesi DB backup):** Postgres kolonu jsonb→text'e cast olur; mevcut diziler JSON-metin string'i olur. `migrateContentToMarkdown` buna dayanıklı (`"["` ile başlayan string'i parse+çevir). jsonb::text cast lossless varsayıldı; yine de **bu schema değişikliğini içeren ilk deploy ÖNCESİ prod DB yedeği al.**
- **Blog/tool içeriğini canlıda nasıl güncellersin (artık):** İçerik markdown olduğu için en kolayı **Strapi panelinden düzenle** (deploy yok). Kod tarafından zorunlu güncelleme gerekiyorsa seed CREATE-only olduğundan idempotent migration pattern'i hâlâ geçerli (`syncBlogFaq` gibi): `findFirst({slug})` → imza karşılaştır → `update({documentId, data, status:'published'})`. AMA bu, o alanı tekrar kod-yönetimli yapar (panel düzenlemesini ezer) — yalnız gerçekten gerekirse.
- **Blog SSS bedava senkronlanır:** `*_SSS` dizileri `syncBlogFaq` (index.ts) ile her boot canlıya imza-karşılaştırmalı yansır. Yeni FAQ sorusu eklemek için ekstra migration GEREKMEZ; sadece diziyi düzenle.
- **Blog gövdesi `icerik` artık MARKDOWN** (üstte FAZ 2). Seed authoring HÂLÂ blocks helper'larıyla (`p, h(level), ul, ol, table, fx(latex→KaTeX), img, pr/t/b/a` — seed/index.ts başı) yazılır, ama atama anında `blocksToMarkdown(...)` ile markdown'a çevrilir. H2/H3/H4 `MarkdownContent`'te auto-anchor (`slugify`) alır.
- **TOC artık H2+H3 gösterir (girintili):** `extractToc` (lib/blocks.ts) H2+H3 alır, `TocEntry.level` taşır; `TOCSidebar` level===3'ü `pl-6` ile girintiler. JSON-LD başlıklardan etkilenmez.
- **CalculatorField `options` desteği:** registry.ts `CalculatorField`'a `options?:{label,value}[]` eklendi → `CalculatorCard` bu alanı `<input>` yerine `<select>` (preset) render eder. Tool çekirdeği `donemGunu` parametresini destekliyordu, dönem seçici (yıllık/çeyreklik/aylık) bununla bağlandı.
- **Project:** stokoloji
- **Description:** Türkçe stok/üretim yönetimi sitesi. **Next.js 15 (web) + Strapi 5 (CMS) + PostgreSQL 16**, pnpm monorepo, Docker Compose ile tek komutta ayağa kalkar.
- **CANLI (2026-06-14):** Coolify ile deploy edildi. web `stokoloji.com`, cms `cms.stokoloji.com` (ayrı subdomain — medya URL'leri için şart). Otomatik SSL (Coolify Traefik). Kaynak: `docker-compose.prod.yml` + prod Dockerfile'lar.
- **Deploy yapısı:** Coolify'da **Docker Compose** build pack (Nixpacks DEĞİL — 3 servis + volume + internal network var). Prod Dockerfile'lar dev'den ayrı: web `next build`+`start`, cms `strapi build`+`start`. `caddy` profili prod'da kullanılmaz (Coolify kendi proxy'sini getirir).
- **Analytics mimarisi:** tek merkezi `lib/analytics/track(event, payload)` → consent'e göre **GA4 + Clarity ikisine de** dağıtır (provider adaptörleri `lib/analytics/providers/`). Component'ler `track()` çağırır; `gtag`/`clarity`'yi direkt çağırmaz. `data-track` attribute'u tek başına event ATMAZ.
- **Analytics consent-gated:** GA4/Clarity script'leri SADECE "Kabul Et" sonrası yüklenir → SSR HTML'de yokturlar (normal, hata değil). Google'ın otomatik tag doğrulaması bu yüzden tag'i "bulamaz"; gerçek doğrulama: Kabul Et → DevTools Network `collect` / GA4 Realtime/DebugView.
- **Env:** `NEXT_PUBLIC_*` build-time bundle'a gömülür → Coolify'da **Build Variable** olarak da girilmeli (runtime yetmez). Strapi secret'ları (`APP_KEYS` vb.) **runtime** (build variable değil).
- **GA4:** `G-3TQF6XG6XX`, Clarity: `x6l7nkaqw5`, Search Console: DNS (CNAME) doğrulaması. `.env.local` (gitignore) local dev için.
- **next/image** `AuthorBox`'ta aktif → prod Strapi medya host'u `next.config.mjs`'te `NEXT_PUBLIC_STRAPI_URL`'den türetilir (remotePatterns).
- **web tsconfig `noUncheckedIndexedAccess` AÇIK** — dizi index erişimi (`arr[0].x`) `possibly undefined` verir; testlerde/kodda `arr[0]!.x` veya guard kullan.
- **Public read izni otomatik:** yeni bir Strapi collection-type eklerken `apps/cms/src/index.ts` `setPublicReadPermissions` içindeki `permissionsByModel`'e `['find','findOne']` ile EKLE → `docker compose up` sonrası web sıfır manuel adımla okur. (Banner için yapıldı.)
- **Hero = tam genişlik banner carousel** (`HeroCarousel.tsx`, `lib/banners.ts`). Veri: `Banner` collection-type (`api::banner.banner`) — kendi metni yok, bir `blog` VEYA `arac` relation'ına bağlanır; başlık/excerpt/link `resolveBannerSlide()` ile o referanstan türer. Görsel önceliği: `banner.gorsel` → `blog.kapakGorseli` → degrade. Eski inline hero + `HeroSignature` (EOQ canvas) ve yarım kalan `content.banner`/`bannerlar`/`PromoBanner`/`BannerStrip`/`HeroBanner` KALDIRILDI.

- **Heading hiyerarşisi kuralı:** İçerik outline'ını YALNIZ gerçek içerik belirler (H1 = sayfa başlığı, H2/H3 = bölümler). Chrome'da seviye atlama yasak: footer kolon başlıkları `<h2 className="footer-col-title">` (a11y için heading kalır, atlamaz), header mega menü dropdown etiketi `<p className="mega-heading">` (içerik başlığı değil). CSS bu class'ları hedefler (tag değil). Anasayfada carousel aktifken görünür H1 yok → `<h1 className="sr-only">` ile tek H1 verilir (fallback hero'da zaten görünür H1 var).
- **JSON-LD yerleşimi:** Organization + WebSite şeması site geneli, `layout.tsx`'te bir kez (her sayfa). Sayfa-özel: blog detay `Article`+`BreadcrumbList`(+`sss` varsa `FAQPage`); tool detay `WebApplication`+`FAQPage`+`BreadcrumbList`; liste sayfaları `CollectionPage`(+ItemList)+`BreadcrumbList`. Builder'lar `lib/seo/jsonld.ts`.
- **api-client `Blog` tipi Strapi schema ile elle senkronize** — schema.json'da alan olması tipte olduğu anlamına gelmez (`sss` blog schema'da vardı ama `types.ts` `Blog`'da yoktu, eklendi). Yeni alan populate ederken hem `BLOG_POPULATE`/`TOOL_POPULATE` hem `types.ts`'i güncelle.
- **Strapi blocks zengin metin TABLO desteklemez** (panel editörü table node üretmez). Çözüm: seed'de custom `{type:'table', headers, rows}` node + `BlocksRenderer`'da `case 'table'` (gerçek `<table>` render → SEO/AI/erişilebilir). `seed/index.ts` blocks helper'ları: `p/h/ul/ol/table/img` + inline `t/b/code/a` + `pr(...children)` (zengin paragraf = link/bold karışık). Hepsi `: any` (generated `BlocksValue` ile uyuşmaz).
- **Blocks içinde web public görseli:** image node `image.url` `/img/...` ile başlıyorsa `BlocksRenderer` `mediaUrl()` ÇAĞIRMAZ (Strapi host eklemez), direkt kullanır. Strapi medyası `/uploads/...` → `mediaUrl` host ekler. SVG/statik görseller `apps/web/public/img/`'e.
- **SEO içerik kaynağı `../launch-icerik/`** (repo dışı, bir üst klasör): EOQ blog+tool markdown'ı hazır (Ali'nin sesi, deneyim paragrafı, em dash yok). Seed içeriği buradan blocks'a çevrilir; sıfırdan yazma. Semrush TR bulgusu: niş küçük ama rekabet düşük; SERP'i otomatik-çeviri sayfalar dolduruyor (native fırsat); Türkler İngilizce de arıyor (`eoq formula` 70) → içerik iki dilli terim kapsar.

- **"Editörün Seçtikleri" = Strapi-yönetilen, ZATEN kuruluydu.** Veri: `Anasayfa` single type'ın `oneCikanYazilar` relation'ı (oneToMany → blog). `page.tsx` ilk yazıyı büyük kart, `slice(1,4)`'ü yan kart yapar → **layout kapasitesi 4 yazı (1 büyük + 3 yan), fazlası sessizce kırpılır**. Kapak/başlık/excerpt (=`seo.description`) blog'un kendisinden populate edilir (`HOME_POPULATE`). Alan boşsa fallback = TÜM yazılar (`posts`). Seed sadece `eoq-nedir`'i koyar.
- **Strapi field açıklaması (edit görünümünde alanın altındaki ipucu) `schema.json`'da TUTULMAZ** — content-manager konfigürasyonunda (DB, core_store key `plugin_content_manager_configuration_content_types::<uid>`) yaşar. Kod ile set etmek için: `strapi.store({type:'plugin', name:'content_manager_configuration', key:'content_types::api::<x>.<x>'})` → `get()` (Promise<unknown>, cast et) → `config.metadatas.<field>.edit.description` ayarla → `set({value:config})`. Bootstrap'ta idempotent + try/catch (config henüz oluşmamış olabilir → boot'u bozma). Örnek: `setAnasayfaFieldHints` (`apps/cms/src/index.ts`). `schema.json` `info.description` ise her zaman görünür ama content-type düzeyinde (alan altında değil) → fallback olarak kullan.

- **Görselli blog'u seed'leme deseni (2026-06-17, `seedEmniyetStogu`):** (1) Gövde görselleri = `img('/uploads/<dosya>.jpg', alt, caption)` blok node'u; Strapi public middleware `/uploads/<dosya>`'yi DB kaydı OLMADAN statik servis eder, `BlocksRenderer` `mediaUrl()` ile host ekler → çalışır. (2) Kapak (`kapakGorseli` = media RELATION) DB kaydı ŞART; upload-service'in formidable file-shape belirsizliğine girme, bunun yerine zaten container'da olan dosyaya işaret eden bir satır yarat: `strapi.db.query('plugin::upload.file').create({data:{name,hash,ext:'.jpg',mime:'image/jpeg',size,width,height,url:'/uploads/<dosya>.jpg',provider:'local',folderPath:'/'}})` → `kapakGorseli: file.id`. `findOne` ile idempotent yap, try/catch ile boot'u bozma. (3) Yeni seed fn → `export` + `apps/cms/src/index.ts` import + bootstrap'a `await`. Blog detay route'u `/icerik/<slug>` (eski `/blog/<slug>` 404).

- **Newsletter mimarisi (2026-06-17): Listmonk self-host + Amazon SES SMTP relay.** Tek giriş noktası `apps/web/src/lib/newsletter/listmonk.ts` (SUNUCU tarafı; token tarayıcıya gitmez — Strapi için api-client neyse newsletter için bu). Akış: form → `useNewsletterSubscribe` hook → `POST /api/newsletter` (route handler, `force-dynamic`) → `subscribe()` → Listmonk `POST /api/subscribers` (`Authorization: token user:token`, `preconfirm_subscriptions:false` → double opt-in onay e-postası KVKK). Tek reusable `NewsletterForm.tsx` tüm noktalarda (anasayfa lead magnet + footer); `LeadMagnetForm` view-tracking'i tutup submit'i ona delege eder. **GRACEFUL (analytics deseni): env (`LISTMONK_API_URL/USER/TOKEN/LIST_ID`) eksikse `isNewsletterConfigured()` false → `subscribe()` `disabled` döner → route 200, form sessizce başarı gösterir, hata yok.** Track: form başarıda `newsletter_submit` (generic), lead magnet ayrıca `lead_magnet_submit` (`onSuccess`). 409 = zaten kayıtlı = başarı. Docker: `listmonk` + `listmonk_db` (ayrı postgres) servisleri; image PULL (build yok); web → `listmonk:9000` internal.

- **Sözlük özelliği (2026-06-17):** Strapi `sozluk-terimi` collection-type (kelime, slug, baslangicHarfi=string, kisaTanim, anlam=blocks, ilgiliTerimler=manyToMany self, seo=seo.meta). Web: `/sozluk` (harf indeksi + gruplu liste) + `/sozluk/[harf]` (inline tam tanım). **Terim başına ayrı sayfa YOK** (kullanıcı kararı: thin content riski) — tanımlar harf sayfasında inline, `slug` anchor/deep-link (`/sozluk/s#stok`) ve ileriye dönük detay için. **URL Türkçe karaktersiz:** diakritik harfler ASCII base harfe katlanır (`Ç+C → /sozluk/c`), `baslangicHarfi` tam Türk harfi saklar; eşleme `harfToSlug`/`harflerForSlug`/`TR_ALPHABET` (api-client'tan export, web `lib/sozluk.ts` re-export + `groupByHarf`/`distinctHarfSluglari`). `listSozlukByHarf` ASCII harfi `$in` ile Türk harf kümesine çevirip filtreler. JSON-LD: `definedTermSetJsonLd` (schema.org DefinedTermSet/DefinedTerm) + BreadcrumbList. Seed: `seed/sozluk-data.ts` (95 terim, helper'lar `seed/sozluk-blocks.ts`) + `seed/sozluk.ts` `seedSozluk` (iki geçişli idempotent: önce terimler, sonra ilgiliTerimler ilişkisi documentId ile). Nav: header primary + drawer + footer. anlam içine yalnız 4 çekirdek konuya (EOQ/emniyet/SDH/ROP) iç link.

- **Listmonk v6 API user oluşturma (2026-06-17, prod):** Listmonk v6.1.0'da **süper admin Basic Auth ile API erişimi KALDIRILDI** (`invalid API credentials`); API çağrısı için ayrı bir `type='api'` user gerekir. UI'a girmeden DB'den oluşturulabilir: `users` tablosuna insert (`type='api'`, `password_login=false`, `user_role_id=1` Super Admin, `status='enabled'`, `email` unique). **Token `password` kolonunda PLAINTEXT** tutulur (`auth.go` `GetAPIToken` → `subtle.ConstantTimeCompare`, bcrypt DEĞİL — admin'in kendi şifresi bcrypt ama API token değil). Insert sonrası **listmonk container'ı RESTART şart** (API user'lar boot'ta belleğe cache'lenir, `CacheAPIUsers`; DB'ye direkt yazınca cache bilmez). Auth header: `Authorization: token <user>:<token>`. Coolify container adları: `listmonk-<hash>`, `listmonk_db-<hash>`, `web-<hash>` (servis adı prefix + `^` ile grep'le). Liste API ile: `POST /api/lists {name,type:public,optin:double}`. Web→listmonk internal erişim servis adıyla çalışır (`http://listmonk:9000`).

## Do-Not-Repeat

<!-- Mistakes made and corrected. Each entry prevents the same mistake recurring. -->
<!-- Format: [YYYY-MM-DD] Description of what went wrong and what to do instead. -->
- [2026-06-17] **`disabled` durumu kullanıcıya başarı gösteriyor → sessiz veri kaybı.** Newsletter env (`LISTMONK_API_*`) Coolify web servisinde boşken form "Teşekkürler" diyordu ama hiçbir kayıt olmuyordu (`subscribe()` → `readConfig()` null → `disabled` → route 200 → form başarı). Teşhis: canlı `curl POST /api/newsletter` → `{"status":"disabled"}`. Ders: form çalışıyor görünse bile gerçek ESP kaydını doğrula; `disabled`/`error`/`invalid` durumlarını canlıda test et. (İyileştirme fırsatı: `disabled`'da kullanıcıya nötr/uyarı göster, sessiz başarı değil.)
- [2026-06-19] **Dev compose'da yeni npm bağımlılığı container'a girmiyor (anon volume gölgelemesi).** `apps/web` dev servisi node_modules'u ANONİM VOLUME tutar (`- /repo/node_modules`, `- /repo/apps/web/node_modules`). Host'ta `pnpm add` + `docker compose build web` yapmak YETMEZ; eski anon volume image'daki yeni node_modules'u gölgeler → `Module not found` (react-markdown, katex/dist/katex.min.css). Çözüm: `docker compose up -d -V web` (`--renew-anon-volumes`) ile anon volume'ları image'dan yenile. Yeni dep eklerken dev container'da DAİMA -V kullan.
- [2026-06-19] **Strapi alan tipini json→richtext (veya tip değişimi) yaparken eski "dizi yazan" migration'lar ValidationError verir.** json→richtext sonrası alan STRING; blocks dizisi yazan her migration (`migrateFormulaBlocks`/`migrateInternalLinks`/`syncBlogFaq` body-strip gibi) yup validation'ı patlatır → boot crash. Tip değiştirmeden önce o alana yazan TÜM migration'ları gözden geçir; tek bir "yeni-tipte-yazan" migration bırak (`migrateContentToMarkdown`).
- [2026-06-19] **blocks→markdown bold dönüşümünde sondaki boşluk bold'u kırar.** `**Kaynak: **` (kapanış `**`'tan önce boşluk) CommonMark'ta emphasis değil → `**` düz metin sızar. Converter'da bold/italic sararken lead/trail boşluğu işaretlerin DIŞINA al.

- [2026-06-14] SVG dosyalarında (örn. `app/icon.svg`) XML yorumlarında `--` (çift tire, örn. `--ink`/`--teal`) KULLANMA. `<!-- -->` içi `--` içeremez → SVG parse edilmez → favicon gelmez (globe fallback). Yorumları sade tut veya kaldır.
- [2026-06-14] Coolify'da Strapi deploy'unda secret env'leri (`APP_KEYS`, `API_TOKEN_SALT`, `ADMIN_JWT_SECRET`, `TRANSFER_TOKEN_SALT`, `JWT_SECRET`, `ENCRYPTION_KEY`) ATLAMA → Strapi boot etmez, crash-loop (`App keys are required`). Hepsi zorunlu.
- [2026-06-14] Prod cms Dockerfile'da `strapi build` öncesi `strapi ts:generate-types` ŞART — `types/generated/` gitignore'da, fresh image'da yok; olmadan seed gibi tipli dosyalar `iliskiliYazilar` vb. alanlarda TS hatası verir. Ayrıca typegen Strapi'yi boot ettiği için `mkdir -p public/uploads` gerekir (upload plugin klasörü arar).
- [2026-06-14] Background/pipe'lı build'de "exit 0" YANILTICI olabilir (tail pipe'ın çıkışı, build'in değil). Gerçek exit'i `; echo EXIT=$?` ile AYRI doğrula.
- [2026-06-14] Coolify BuildKit gerçek `next build`/`strapi build` stdout'unu gizleyip sadece `Dockerfile:line` + `exit code 1` gösterebilir. Gerçek hata için: VPS'te `docker build -f ... .` ELLE çalıştır, ya da kaynağı (RAM/OOM gibi) çözünce build ilerleyip gerçek hata ortaya çıkar.
- [2026-06-16] **Strapi TS build `noEmitOnError` ile çalışır: cms'te TEK bir TS hatası TÜM emit'i bloklar.** Sonuç sinsi: yeni eklenen content-type API'si kaydolmaz (`/api/<plural>` → **404**), bootstrap/seed eski dist'le çalışır (yeni seed fn'i hiç loglamaz), `dist/src/api/` stale kalır. Debug: `ls apps/cms/dist/src/api` ile yeni API'nin derlenip derlenmediğine bak. Yeni Strapi işi sonrası `cd apps/cms && npx strapi ts:generate-types && npx tsc --noEmit -p tsconfig.json` ile DERLEMEYİ doğrula (web typecheck cms'i KAPSAMAZ). `blocks` alanı için elle JSON node kuran helper'lar generated `BlocksValue` ile uyuşmaz → dönüşü `: any` işaretle.
- [2026-06-16] **`docker compose up` çalışan cms container'ını "Recreated" etmeyip "Running" bırakabilir** → kod değişikliğin YANSIMAZ (eski container). İlk up'taki 404'lere aldanma; `docker compose up --build` ya da `docker compose restart cms` ile taze başlat.
- [2026-06-16] **Strapi `blocks` alan tipine ELLE custom node (örn. `type:'table'`, veya `/img/` url'li `type:'image'`) seed'leme.** Strapi 5 blocks validator'ı create sırasında bilinmeyen/uyumsuz node tiplerini reddeder (`ValidationError: Block node is of invalid type`) → bootstrap seed CRASH-LOOP. Bu bug seed idempotent olduğu için LATENT kalır (eski DB'de atlanır); sadece taze DB rebuild'inde patlar. İçerik seed/markdown'dan gelip web'de custom `BlocksRenderer` ile render ediliyorsa alanı `blocks` DEĞİL `json` yap (validation yok, tablo/görsel custom node'ları geçer). Şema değişince cms image rebuild + DB volume reset şart (cms dev compose source mount etmez, schema build-time gömülür). Bkz bug-053.
- [2026-06-16] **Yerel `/araclar/<slug>` test ederken slug'ı registry'den DOĞRULA.** Stok devir hızı tool slug'ı `stok-devir-hizi-hesaplama` (sadece `stok-devir-hizi` değil) → yanlış slug 404 verir, gerçek bir bug sanma. Kaynak: `apps/web/src/lib/tools/registry.ts` + Strapi `slug` alanı (ikisi eşleşmeli).
- [2026-06-16] **Strapi medyasında `next/image` KULLANMA — docker dev'de 500 verir.** `mediaUrl` tarayıcı için `http://localhost:1337` üretir; next/image optimizer görseli SUNUCU tarafında (web container) çeker, oradan `localhost:1337` cms'e değil web'in kendisine gider → ECONNREFUSED → `/_next/image ... 500`. Prod'da host `cms.stokoloji.com` (her iki taraftan erişilir) olduğu için fark edilmez. Çözüm: Strapi görsellerini düz `<img>` ile yükle (projede Thumb/blog zaten böyle); LCP için ilk görselde `loading="eager"` + `fetchPriority="high"`, `// eslint-disable-next-line @next/next/no-img-element`. (HeroCarousel'de uygulandı.)
- [2026-06-16] **Global `sed 's|/blog|/icerik|g'` path rename'inde over-replace tuzağı:** path'in slug segmenti de eski adla aynıysa (`/blog/blog`) sed ikisini de değiştirir (`/icerik/icerik`). banners.test.ts'te oldu. sed sonrası MUTLAKA `pnpm test` çalıştır. Genelde route rename'i `git mv` + sed + test ile yap.
- [2026-06-17] **`apps/cms/public/uploads` bir NAMED VOLUME (`strapi_uploads`), host klasörünü GÖLGELER.** Host'a (`apps/cms/public/uploads/`) elle konan görseller container içinde GÖRÜNMEZ → seed'in diskten okuması ve `/uploads/<dosya>` statik servisi 404. Çözüm: `docker cp <host_dosya> comstokoloji-cms-1:/repo/apps/cms/public/uploads/<dosya>` ile volume'e kopyala (VEYA Strapi upload service ile yükle). Kopyaladıktan sonra `curl -o /dev/null -w '%{http_code}' http://localhost:1337/uploads/<dosya>` ile 200 doğrula.
- [2026-06-17] **Listmonk v6 API erişimi superadmin parolasıyla OLMAZ.** Superadmin (`LISTMONK_ADMIN_USER/PASSWORD`) yalnız web paneli oturumu içindir; `/api/*` BasicAuth/`token` erişimi için panelde (Admin > Users) ayrı bir **API user + token** üretmek ŞART. Yanlış kullanım `403 {"message":"invalid API credentials"}` verir, bir bug sanma. Header iki biçimi de destekler: `Authorization: token <user>:<token>` (tercih) veya `curl -u user:token` BasicAuth. Liste ID'si liste detayında görünür; `LISTMONK_LIST_ID`'ye yazılır.
- [2026-06-17] **Blog detay canlı route'u `/icerik/<slug>`, `/blog/<slug>` DEĞİL.** `apps/web/src/app/(site)/icerik/` var, `blog/` yok. Seed/markdown'da blog'a iç link verirken `/icerik/` kullan; `/blog/` linkleri 404. Eski emniyet seed kayıtlarında 2 `/blog/` link kırıktı → bootstrap'a idempotent `migrateInternalLinks` (`apps/cms/src/index.ts`) eklendi: tüm blog.icerik + tool.formulAciklamasi bloklarını recursive dolaşır, `type:'link'` url'lerinde `/blog/`→`/icerik/` çevirir. [[idempotent-seed-migration deseni, bkz line 66]]. Tool sayfası route'u `/araclar/<slug>` (doğru). Doğrulama: `grep 'href="/blog/' <fetched-html>` cluster genelinde 0 olmalı.
- [2026-06-17] **Görsel sıkıştırma: `sharp` YOK (web/cms/container hiçbirinde require edilemiyor) ama `python3 -m PIL` (Pillow) VAR.** Kullanıcının verdiği 6MB / 2848px PNG'leri prod'a koyma (CWV kırmızı çizgi). Pillow ile optimize JPEG: `Image.open(src).convert('RGB').resize((1600,h),LANCZOS).save(dst,'JPEG',quality=82,optimize=True,progressive=True)` → 6MB → ~70KB. SVG rasterize gerekirse `/Applications/Google Chrome.app/.../Google Chrome --headless --screenshot=out.png --window-size=W,H --force-device-scale-factor=2 file://...` (Chrome screenshot her zaman PNG; JPEG için Pillow'dan geçir). Görselleri `apps/cms/public/uploads/`'a koyup [[named-volume gotcha]] gereği `docker cp ... comstokoloji-cms-1:/repo/apps/cms/public/uploads/` ile container'a da kopyala.
- [2026-06-17] **Seed'e SONRADAN kapak/görsel eklerken guard'ı "backfill-once" yap.** İlk seed görselsiz oluştuysa kayıt zaten var → düz guard atlar, görsel hiç bağlanmaz; destructive silme de zahmetli (`strapi console` 1337'yi bağlamaya çalışır → "port already used"; `docker compose run --rm` REPL'i piped IIFE'yi güvenilir çalıştırmaz). Çözüm: findFirst'e `populate:['kapakGorseli']` ekle; guard `existing && existingTool && existing.kapakGorseli && existingTool.kapakGorseli` ise atla; değilse tool/blog branch'lerinde `else if (!existing.kapakGorseli)` ile kapağı (blog'da ayrıca `icerik: ROP_BLOG` → gövde img node'u) BİR KEZ backfill et. Kapak bağlanınca sonraki boot'ta yakınsar (atlar) → kullanıcının admin'deki elle düzenlemesi EZİLMEZ. `seedRop` (`apps/cms/src/seed/index.ts`) örnek.
- [2026-06-17] **Blog `sss` (FAQ) alanı blog detayda GÖRÜNÜR render edilir** (`icerik/[slug]/page.tsx`): `<details className="faq-item">` akordeon, prose'dan SONRA + ilgili-tool CTA'dan ÖNCE. Tek alan hem `faqPageJsonLd(post.sss)` (JSON-LD) hem görünür FAQ'ı besler — gövdeye Q&A YAZMA, `sss` alanına yaz. Tool sayfası zaten aynı `.faq-item` desenini kullanıyor (`ToolPageTemplate`). Gövdeye "Sıkça/Sık sorulan sorular" başlığı KOYMA (görünür FAQ ayrı bölüm; çift başlık olur). FAQ içeriği Semrush `phrase_questions` (us zengin, tr seyrek/ERROR 50) + Google PAA (WebSearch) harmanından.
- [2026-06-17] **Mevcut kayıtların `sss`'ini güncellemek: `syncBlogFaq` migration** (`apps/cms/src/index.ts`, bootstrap). Idempotent seed mevcut kaydı atlar; FAQ'ı canlıya taşımak için canonical `export const BLOG_SSS/SDH_SSS/EMNIYET_SSS/ROP_SSS` (seed/index.ts'ten import) → slug→sss map → soru+cevap imzası farklıysa `update`. Aynı migration gövdedeki artık FAQ başlığını (`/^s[ıi]k(ça)? sorulan sorular$/i`) + takip eden "Aşağıda..." giriş paragrafını da temizler. NOT: bu 4 launch blog FAQ'ı SEED-YÖNETİMLİ — panelden elle düzenleme sonraki boot'ta canonical ile geri yazılır (kapak/içerik backfill'in aksine; FAQ kasıtlı seed-driven). Yakınsar (imza eşitse dokunmaz).
- [2026-06-16] **Anasayfa title'ı `title.absolute` ile EZME** — layout default `'Stok Yönetimi Araçları ve Rehberleri | Stokoloji'` (head term "Stok Yönetimi" öne, cerebrum SEO kararı) doğrudur; anasayfada title verme, sadece description + OG ver. Brand'i ("Stokoloji —") öne almak head term'i geri iter.
- [2026-06-17] **Strapi `enumeration` alanına ham Türkçe harf koyma — normalize çakışır.** Strapi enum değerlerini normalize ederken diakritik strip + uppercase yapar; Ç→C, Ğ→G, İ/ı→I, Ş→S çakışır → `Content Type Definition is invalid ... values collide when normalized` → `ts:generate-types`/boot fail. Türk alfabesi harf alanı için `type:'string'` kullan (gruplama/filtre exact string `$in` ile zaten çalışır), enum gerekiyorsa ASCII-benzersiz değerler. Bkz bug-056. (Sözlük `baslangicHarfi` alanı.)
- [2026-06-17] **Idempotent seed ESKİ DB'yi güncellemez** (slug guard → varsa atlar). İçerik şekli değişince (formül `pr(code())` inline → `fx()` code-block) yeni seed yalnız taze DB'de etki eder; mevcut local/PROD kaydı eski kalır. Çözüm: bootstrap'a **idempotent migration** (`migrateFormulaBlocks`, `apps/cms/src/index.ts`) — `strapi.documents(uid).findMany({status:'published'})` → JSON içeriği yerinde dönüştür → `update({documentId, data, status:'published'})`. Idempotent guard = dönüştürülen node'un yeni tipi. cms restart bootstrap'ı çalıştırır; PROD'da redeploy. DB reset/admin kaybı YOK. **Formül convention:** standalone formül = seed'de `fx(latex)` / Strapi'de `code` block → BlocksRenderer backslash'lıysa KaTeX display, düz metinse `.formula-block--text` kartı; inline `$...$` → KaTeX inline. KaTeX matematik fontu Türkçe glifte zayıf → kelimeli formüller (`\text{Türkçe}`) text kalır, label `SS`.

- **Görsel binary'lerini PROD'a taşıma (Approach A, sürdürülebilir):** `public/uploads` kalıcı named volume → image build'indeki dosyaları GÖLGELER, fresh prod deploy'da BOŞ başlar → seed file-record'ları + gövde görselleri 404. Çözüm: binary'leri git'li `apps/cms/seed-assets/uploads/`'a koy (`.dockerignore` sadece `public/uploads`'ı dışlar, `.gitignore` sadece `public/uploads/*` → seed-assets TAKİP EDİLİR; prod Dockerfile `COPY apps/cms apps/cms` ile image'a girer), bootstrap'ta `ensureUploadAssets` (index.ts) çalışma anında `seed-assets/uploads/* → public/uploads/*` kopyalar (yoksa, idempotent), **seed'lerden ÖNCE**. Path `process.cwd()` bazlı (`/repo/apps/cms` hem dev hem prod). `fs/promises` (access/copyFile/mkdir/readdir). Volume'dan host'a çıkarmak: `docker compose cp cms:/repo/apps/cms/public/uploads/. apps/cms/seed-assets/uploads/` (admin-upload responsive varyantları large_/medium_/small_/thumbnail_ dahil hepsi). Test: volume'dan dosya sil → cms restart → log "N kopyalandı" + 200. **İçerik=kod akışı:** yeni yazı prod'a = seed commit + Coolify redeploy; idempotent seed eskiyi korur, ensureUploadAssets görseli getirir.

## Decision Log

<!-- Significant technical decisions with rationale. Why X was chosen over Y. -->

- [2026-06-17] **ESP kararı: Brevo İPTAL → Listmonk (self-host) + Amazon SES (SMTP relay), eu-central-1 (Frankfurt).** Sebep: KVKK için veri kontrolü (abone listesi kendi DB'mizde, AB bölgesi), maliyet (SES ~$0.10/1000 e-posta, Listmonk ücretsiz), spam/teslimat (kendi domain DKIM/SPF/DMARC + SES IP itibarı). Brevo'nun barındırılan listesi + fiyatlandırması yerine. SES DNS (DKIM 3xCNAME + custom MAIL FROM `mail.stokoloji.com` MX+SPF + DMARC) girildi. **Kalan panel işi: SES sandbox'tan çık (production access) + SMTP credential üret; Listmonk panelinde Settings>SMTP (`email-smtp.eu-central-1.amazonaws.com:587` STARTTLS) + API user/token + liste (double opt-in).** Eski Brevo memory'si güncellendi.
- [2026-06-17] **Formül dizgisi = KaTeX (server-side), convention: code-fence içeriği.** Eski hâl: formüller markdown code-fence → Strapi `code`/inline `code` → düz gri monospace `√L`/`σ_d` ASCII taklit (otorite sitesine yakışmıyor). Karar: KaTeX, **`katex.renderToString` ile SSR/build'de HTML** (client JS yok → CWV korunur, kural 7). `BlocksRenderer` `code` bloğu kuralı: **backslash içeriyorsa gerçek LaTeX → KaTeX display**; düz metinse (kelimeli Türkçe formül) → şık metin kartı (`.formula-block--text`), KaTeX DEĞİL. Sebep: KaTeX matematik fontu (Computer Modern) Türkçe glifte (ğ/ş/ı dotless) zayıf, `\text{Türkçe}` tofu riski → kelimeli formüller (ROP "Ortalama günlük talep × ...") text kalır, sembolik olanlar (σ/√/subscript) LaTeX olur. Label için `SS` kısaltması (içerikte zaten kullanılıyor) — `\text{Emniyet Stoğu}` glif riskinden kaçınmak için. Satır-içi `$...$` → KaTeX inline. Türkçe ondalık virgül LaTeX'te `1{,}65` (sıkı aralık). Launch-icerik emniyet+EOQ fence'leri LaTeX'e çevrildi; ROP kelimeli kaldı.

- [2026-06-16] **SEMrush (tr) düzeltmesi:** "stok yönetimi" head term'i ulaşılabilir — hacim 320, **KD 17 (çok kolay)**; "ERP devleri domine, girilemez" varsayımı yanlıştı (markalar ticari intent sayfalarına yatırım yapıyor, bilgi sorgusunda zayıf). Anasayfa title/H1 "Stok Yönetimi" ile başlıyor → doğru. Ama "stok yönetimi hesaplama" ve "stok yönetimi araçları" tam ifadeleri **~0 hacim** → title kısaltıldı: "Stok Yönetimi Araçları ve Rehberleri | Stokoloji". Hacim sıralaması (tr/ay): stok devir hızı hesaplama **720** (KD21, en büyük tek fırsat, tool sayfası — roadmap'te öne çek), stok devir hızı 390, stok yönetimi 320, abc analizi 140, eoq hesaplama 10 / emniyet stoğu / güvenlik stoğu / hesaplama hepsi **KD 0 = sıfır rekabet** (launch cluster doğru seçilmiş, girince rank eder).
- [2026-06-16] **Anasayfa SEO = "hesaplama araçları" şemsiye intent'i, head term DEĞİL.** Title/H1/H2 "stok yönetimi" head term'inden (Logo/Mikro/SAP/ERP markaları domine, ticari/SaaS intent, ulaşılmaz) çekildi; spesifik `X hesaplama` term'leri tool sayfalarına bırakıldı (cannibalization önleme). Title: "Stok Yönetimi Hesaplama Araçları ve Rehberleri | Stokoloji"; H1 (sr-only): "Stok yönetimi hesaplama araçları ve mühendislik rehberleri". Rakip katmanları ayrı: tool rakipleri çok az (HarmonyERP/Vepara/Mikro yalnız stok devir hızında), blog rakipleri kalabalık ama hepsi yazılım markası (Adisyo/Paraşüt/Multinet/ikas) — mühendislik açısı hiçbirinde yok. Not: emniyet stoğu = güvenlik stoğu = minimum stok, üçü de aranıyor, içerikte üçü de geçmeli.
- [2026-06-14] **Deploy = Coolify + Docker Compose build pack** (`docker-compose.prod.yml`). Nixpacks değil: tek servis varsayardı, db+cms'i kaçırırdı. Compose 3 servisi, kalıcı volume'ları (`pgdata`+`strapi_uploads`) ve internal network'ü (`http://cms:1337`) yönetir.
- [2026-06-16] **Arama = client-side filtre** (`SiteHeader` `nav` prop'u üzerinde, Türkçe normalize: ı/i + diacritic strip). Meilisearch reddedildi: bu ölçekte (3 tool + 3 blog) ayrı container + RAM (VPS zaten OOM sınırında), index pipeline'ı, plugin bakımı = aşırı mühendislik. nav zaten Strapi'den geliyor, ek istek yok, CWV/consent maliyeti sıfır. İçerik binlere çıkıp typo-tolerance/faceting gerekince Meilisearch'e geçiş ~1 günlük iş, o zaman yapılır.
- [2026-06-14] **VPS 6 GB RAM yetmedi** (boştayken ~2 GB available); eşzamanlı `next build`+`strapi build` OOM yapıyordu (çıktısız exit). Çözüm: sunucuya **8 GB swap** (`/swapfile_build`, fstab'a eklendi). Sunucu büyütmek yerine swap — ucuz, build'i öldürmez.
- [2026-06-14] **cms ayrı subdomain** `cms.stokoloji.com` — tarayıcı Strapi medya URL'lerini buradan çeker; aynı domain'de servis edilmez.
- [2026-06-14] **Search Console = DNS (CNAME) doğrulaması** (meta yöntemi `layout.tsx`'te `verification.google` + `GOOGLE_SITE_VERIFICATION` env olarak da hazır ama kullanılmadı).
- [2026-06-14] **Çift `@types/react`** (web@19 vs cms@18) → root `package.json` `pnpm.overrides` ile `@types/react` 19.2.17 + `@types/react-dom` 19.2.3 sabitlendi.
- [ileride] `.xyz` → `.com` 301 (Cloudflare/DNS redirect, içerik servis etmeden).
- [2026-06-16] **Blog route `/blog` → `/icerik` taşındı** (kullanıcı kararı, launch öncesi, indexlenmiş URL yok → 301 gereksiz). Dizin `app/(site)/icerik/`, görünür etiketler "Blog" → "İçerik" (header mega + drawer + breadcrumb), iç state identifier'ları ('blog'|'tools') değişmedi. launch-icerik markdown'larındaki `/blog/...` linkler `/icerik/...`'e maplenir. **2. dalga yazıları (emniyet stoğu, ROP) henüz YOK → onlara link verme (404).**
- [2026-06-16] **Launch kapsamı: önce sadece EOQ mükemmelleştirilir** (kullanıcı kararı). Emniyet stoğu + ROP (tool+blog) = 2. dalga. EOQ blog 7 zorunlu H2 (soru formatı) + tablo + adım adım örnek + deneyim paragrafı + EOQ maliyet grafiği (`public/img/eoq-maliyet-grafigi.svg`) + 5 SSS; tool 5 SSS + formül açıklaması. İçerik girişi: `seed/index.ts` (versiyon kontrollü).
- [2026-06-16] **Lead magnet e-posta toplama: Brevo + Strapi hibrit (DEFERRED — sonra yapılacak).** Strapi'ye özel güvenilir v5 newsletter plugin'i yok (topluluk plugin'leri v4/bakımsız). Strapi'nin kendi resmi blogu Next.js+Strapi+Brevo öneriyor. Karar: veri Strapi'de (`Abone`/`Lead` collection-type: email, kaynak, kvkkOnay, onayZamani, ip — `api-client` üzerinden, liste sahipliği bizde kalır); gönderme/liste/DOI/unsubscribe Brevo'da (EU merkezli=KVKK doğru taraf, ücretsiz 300 mail/gün + 100k kontak + double opt-in + API). Akış: form→Next route handler→Strapi kaydı + Brevo DOI API→onay maili→Excel indirme linki + `track('lead_magnet_submit')`. `BREVO_API_KEY` server env (tarayıcıya sızmaz). İlk lead magnet hazır: `apps/web/public/sablonlar/stokoloji-stok-yonetimi-komuta-paneli.xlsx`. Açık karar: indirme hemen mi (düşük sürtünme) yoksa onay sonrası mı (gated, en temiz KVKK).
- [2026-06-16] **Banner = ayrı collection-type, repeatable component DEĞİL.** Kullanıcı her banner'ın bağımsız yayınla/taslak olmasını istedi → `api::banner.banner` (draftAndPublish). Metin override YOK: editör mevcut blog/araç seçer, görünüm verisi referanstan türer (kısayol). Görsel opsiyonel banner alanı (blog kapağı varsa o, yoksa kullanıcı kendi görselini ekler).

- [2026-06-20] **Sosyal video stratejisi: kısa video (Shorts/TikTok/Reels) → siteye trafik, Postiz ile dağıtım.** Kullanıcı kararı: amaç içerik tüketimi değil, **araç/yazılara tıklama**. Devil's advocate kabul edildi: kısa video platformları outbound link'i bastırır → trafik "kasıtlı merak boşluğu" (video aha verir, tam hesap/şablon siteye saklanır) + bio/sabit-yorum link + **sesli CTA** ile taşınır. Birincil segment **E-ticaret** (linke tıklama refleksi + terime açık + acı ölçülebilir); ikincil KOBİ-toptan/üretim (hikaye/deneyim); öğrenci sosyal videoda hedeflenmez. Üretim kuralı: **ortak gövde (formül+grafik animasyonu) sabit, hook (ilk 2sn) + CTA segmente göre swap** → tek üretim iki varyant. Format 9:16/1080x1920/~15sn/watermark'sız. Marka şablonu lacivert#1B2A4A/krem#F7F3EC/amber#E0A23B + üstte progress bar + sabit end-card. Ölçüm: bio/yorum link'lerine UTM (`utm_source=tiktok|instagram|youtube`, `utm_medium=short`, `utm_campaign=<tool>`, `utm_content=eticaret|kobi-toptan|kobi-uretim`) → GA4 segment kırılımı. **Segment × tool matrisi: `../sosyal-video-segment-matrisi.xlsx`** (repo dışı, parent klasör; 11 video varyantı + UTM kuralı sayfası). Çekim sırası: 1) stok-devir-hizi/eticaret 2) stok-devir-hizi/kobi-toptan (hook-swap) 3) abc/eticaret 4) emniyet-stogu/eticaret 5) eoq/eticaret. Claude ile üretim = animasyonlu HTML artifact (gerçek video dosyası değil) → ekran kaydı → Postiz native upload.

### Key Learning (2026-06-19) — Postiz self-host artık ağır
Postiz v2.12+ Temporal zorunlu; resmi compose Temporal'ı Elasticsearch 7.17 (~1-2GB) + ayrı postgres ile getiriyor. "2GB'de test edildi" iddiası ES öncesi. 6GB VPS için LEAN profil: ES'yi çıkar, Temporal'ı postgres-visibility ile çalıştır (DYNAMIC_CONFIG_FILE_PATH=config/dynamicconfig/development-sql.yaml). Deploy paketi deploy/postiz/ altında. Pipeline (Strapi publish→Postiz API) apps/cms lifecycle'ında olacak (ayrı worker yok=RAM). Postiz tek container port 5000, NEXT_PUBLIC_BACKEND_URL=MAIN_URL+/api. API: Authorization header (key, "pos_" yok); self-host base {MAIN_URL}/api/public/v1.
